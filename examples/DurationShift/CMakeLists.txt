FILE(GLOB SRC *.cpp *.c)
FILE(GLOB HDR *.hpp *.h)

SET(TARGET_NAME "DurationShift")

MESSAGE(" * building ${TARGET_NAME}")

ADD_EXECUTABLE(${TARGET_NAME} ${SRC} ${HDR})
TARGET_LINK_LIBRARIES(${TARGET_NAME} ${MAGE_LIB_NAME})
